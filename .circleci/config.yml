version: 2
jobs:
  build:
    working_directory: ~/app
    docker:
      - image: python:3.4.2
    steps:
      - checkout
      - run: echo "hoge"
        # name: Install Dependencies
        # command: echo "hoge"
        # command: pip install -r reqquirements.txt
